<div class="col-xs-6 col-sm-3 sidebar-offcanvas" id="sidebar" role="navigation">
	<ul class="nav" style="margin-bottom:30px;">
	  <% if @page.has_parent? %>
	  	  <% parent = @page.ancestors.first %>
	  	  <% parent.pages.active.order('display_order ASC').each do |page| %>
	  	  <li class="<%= page == @page ? 'active': '' %>"><%= link_to page.page_title, pages_path(page.page_url), title: page.page_title %></li>
		  <% end %>
	  <% else %>
	  	<% Page.sections.each do |page| %>
	  	  <li class="<%= page == @page ? 'active': '' %>"><%= link_to page.page_title, pages_path(page.page_url), title: page.page_title %></li>
		  <% end %>
	  <% end %>
	</ul>
	<div class="fb-like-box" data-href="https://www.facebook.com/pages/Mariveles-Water-District/182322801812641" data-width="100" data-height="200" data-colorscheme="light" data-show-faces="false" data-header="true" data-stream="true" data-show-border="true"></div>
</div>
